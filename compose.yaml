services:
  seo-api:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    init: true
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
